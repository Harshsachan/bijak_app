// var backResult =Get.to(() => ProductDetailPage(product: product));
// if (backResult != null) {
// setState(() {
// print(cartItems.length);
// cartItems = backResult as List<Product>;
// });
// _saveCartData();
// }